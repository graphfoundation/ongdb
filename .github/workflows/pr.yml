# Legacy scoped PR jobs retained only for transition.
# Prefer pr-dev.yml (dev-quality-gate) on *-dev and full-reactor-public.yml on release lines.
# This workflow no longer runs — disabled via empty paths workaround: use workflow_dispatch only.

name: PR quality gate (deprecated)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Deprecated. Use 'Dev quality gate' (pr-dev.yml) or 'Public full reactor'."
          exit 0
